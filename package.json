{
  "name": "particle-dev",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Kevin Lazich <dev@kevinlazich.com>",
  "license": "MIT",
  "dependencies": {
    "node-red": "^0.18.4",
    "particle-api-js": "^7.2.0",
    "particle-cli": "^1.29.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "dotenv": "^5.0.1",
    "prettier": "1.12.0"
  },
  "scripts": {
    "build": "babel src -d lib",
    "prettier": "prettier --single-quote --trailing-comma es5 --write ./**/*.{js,md,css,json}",
    "start": "node -r dotenv/config lib/index.js",
    "prestart": "yarn build"
  }
}
